# 管理员账号（仅首次启动时创建，后续修改在Web界面操作）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=请修改为强密码

# JWT 签名密钥（务必修改为随机字符串！）
# 生成方式: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=请修改为随机字符串

# 数据库路径（通常不需要改）
DATABASE_URL=sqlite:///./data/app.db
