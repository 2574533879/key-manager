# 客户端认证 API 文档

## 接口

```
POST /api/client/auth
```

**无需认证。路径固定，不受管理端前缀影响。**

---

## 请求参数

```json
{
    "machine_code": "1A2B3C4D5E6F7G8H9I0J1K2L3M4N5O6P",
    "key_code": "YOUR-KEY-CODE",
    "app_code": "APP-CODE",
    "ip_address": "192.168.1.100"
}
```

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| machine_code | string | 是 | 设备唯一机器码（32位 MD5） |
| key_code | string | 是 | 卡密 |
| app_code | string | 是 | 应用编号 |
| ip_address | string | 否 | 设备 IP，不传则使用请求来源 IP |

### 机器码生成（Windows）

```python
import wmi, hashlib, psutil

def get_cpu_info():
    return wmi.WMI().Win32_Processor()[0].ProcessorId

def get_motherboard_info():
    return wmi.WMI().Win32_BaseBoard()[0].SerialNumber

def get_disk_info():
    return wmi.WMI().Win32_DiskDrive()[0].SerialNumber.strip()

def get_mac_address():
    for interface, addrs in psutil.net_if_addrs().items():
        for addr in addrs:
            if addr.family == psutil.AF_LINK and interface != "Loopback Pseudo-Interface 1":
                return addr.address.replace("-", "").upper()

def generate_machine_code():
    raw = f"{get_cpu_info()}_{get_motherboard_info()}_{get_disk_info()}_{get_mac_address()}"
    return hashlib.md5(raw.encode()).hexdigest().upper()
```

---

## 响应

### 成功 (200)

```json
{
    "status": "success",
    "message": "认证成功",
    "key_info": {
        "key_code": "A1B2C3D4E5F6G7H8I9J0",
        "app_name": "测试应用",
        "key_type": "month",
        "start_time": "2025-12-03 10:00:00",
        "expire_time": "2026-01-02 10:00:00",
        "device_limit": 2,
        "used_count": 1
    }
}
```

| 字段 | 说明 |
|------|------|
| key_code | 卡密 |
| app_name | 应用名称 |
| key_type | 类型：day/week/month/quarter/year/permanent/custom |
| start_time | 首次激活时间（北京时间） |
| expire_time | 过期时间 |
| device_limit | 最大设备数 |
| used_count | 当前已绑定设备数 |

### 失败

所有错误返回格式统一为：

```json
{
    "status": "error",
    "message": "错误描述"
}
```

| 状态码 | message | 说明 |
|--------|---------|------|
| 400 | 缺少必要参数：{field} | 必填参数缺失 |
| 400 | 卡密与应用不匹配 | key_code 不属于该 app_code |
| 403 | 卡密已过期 | 超过有效期 |
| 403 | 卡密已被禁用 | 管理员禁用 |
| 403 | 设备数量已达上限（N台） | 绑定设备超限 |
| 404 | 应用编号无效 | app_code 不存在 |
| 404 | 卡密不存在 | key_code 不存在 |
| 429 | 请求过于频繁，请稍后再试 | 频率限制（10次/分钟） |

---

## 代码示例

### Python

```python
import requests

def auth_key(server_url, machine_code, key_code, app_code):
    try:
        resp = requests.post(
            f"{server_url}/api/client/auth",
            json={
                "machine_code": machine_code,
                "key_code": key_code,
                "app_code": app_code,
            },
            timeout=10,
        )
        data = resp.json()
        if data["status"] == "success":
            print(f"认证成功，到期时间：{data['key_info']['expire_time']}")
            return True, data["key_info"]
        else:
            print(f"认证失败：{data['message']}")
            return False, data["message"]
    except Exception as e:
        print(f"网络错误：{e}")
        return False, str(e)
```

### curl

```bash
curl -X POST http://127.0.0.1:8000/api/client/auth \
  -H "Content-Type: application/json" \
  -d '{
    "machine_code": "1A2B3C4D5E6F7G8H9I0J1K2L3M4N5O6P",
    "key_code": "YOUR-KEY-CODE",
    "app_code": "APP-CODE"
  }'
```

---

## 认证流程

```
客户端请求 → 参数校验 → 应用验证 → 卡密验证 → 
归属验证 → 状态检查 → 首次激活（如需要）→ 
过期检查 → 设备绑定/心跳更新 → 返回结果
```

## 注意事项

1. 卡密**首次使用**自动激活，有效期从激活时开始计算
2. 每次认证成功会**更新心跳时间**，管理后台据此判断在线状态
3. 同一设备多次认证**不增加**设备计数
4. 建议客户端每隔 3-5 分钟调用一次作为心跳保活
5. 服务端使用**北京时间**，所有时间字段均为 UTC+8
