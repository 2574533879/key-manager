# 卡密管理系统 — 设计文档

## 1. 项目概述

卡密管理系统是一个面向软件开发者的**许可证密钥管理平台**，用于管理软件的授权卡密（License Key），支持客户端设备绑定认证和管理后台可视化管理。

### 1.1 核心功能

- **管理后台（Web界面）**：仪表盘、应用管理、卡密管理、回收站、系统设置
- **客户端认证 API**：卡密验证、设备绑定、心跳上报
- **安全机制**：JWT 双令牌认证、TOTP 两步验证、动态 API 路由前缀、请求频率限制

### 1.2 适用场景

一位管理员通过 Web 后台管理多个软件产品的卡密授权，客户端软件通过 API 接口进行激活和续期验证。适合个人开发者或小团队使用。

---

## 2. 技术架构

### 2.1 技术栈

| 层级 | 技术 |
|------|------|
| Web 框架 | FastAPI 0.115 |
| 模板引擎 | Jinja2 |
| CSS | Tailwind CSS（build_css.py 从模板提取类名生成，无外部依赖） |
| 数据库 | SQLite（单文件，零配置） |
| ORM | SQLAlchemy 2.0 |
| 认证 | JWT (python-jose) + bcrypt + TOTP (pyotp) |
| 部署 | Docker / Docker Compose（非 root 用户运行） |

### 2.2 系统架构图

```
┌─────────────────────────────────────────────────────┐
│                    客户端 (Client)                    │
│          POST /api/client/auth                      │
│          { machine_code, key_code, app_code }       │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│                 FastAPI 应用服务器                     │
│                                                      │
│  ┌──────────────────┐  ┌──────────────────────────┐ │
│  │  /api/client/*   │  │  {prefix}/*  (管理端API) │ │
│  │  固定路径，无认证  │  │  动态前缀，JWT + TOTP    │ │
│  └──────────────────┘  └──────────────────────────┘ │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │  动态前缀中间件                                 │   │
│  │  {prefix}/xxx → /__admin/xxx (内部重写)       │   │
│  │  阻止 /__admin/xxx 直接访问 (防绕过)           │   │
│  └──────────────────────────────────────────────┘   │
│                                                      │
│  ┌──────────────────────────────────────────────┐   │
│  │  Jinja2 模板 (Web 界面)                        │   │
│  │  /login / /applications /keys /recycle-bin    │   │
│  │  /settings                                    │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│                   SQLite 数据库                       │
│  admin / settings / applications / keys /            │
│  device_bindings / key_recycle_bin                   │
└─────────────────────────────────────────────────────┘
```

---

## 3. 数据库设计

### 3.1 ER 图

```
┌──────────┐       ┌──────────────┐       ┌─────────────────┐
│   admin  │       │ applications │       │      keys       │
├──────────┤       ├──────────────┤       ├─────────────────┤
│ id (PK)  │       │ id (PK)      │◄──────│ app_id (FK)     │
│ username │       │ name         │       │ key_code (UQ)   │
│ password │       │ app_code(UQ) │       │ batch_id (IDX)  │
│ otp_sec  │       │ description  │       │ key_type        │
└──────────┘       │ created_at   │       │ device_limit    │
                   └──────────────┘       │ status          │
                                          │ start_time      │
                   ┌──────────────┐       │ expire_time     │
                   │   settings   │       │ created_at      │
                   ├──────────────┤       └────────┬────────┘
                   │ key (PK)     │                │
                   │ value        │       ┌────────▼────────┐
                   └──────────────┘       │ device_bindings │
                                          ├─────────────────┤
                   ┌────────────────┐     │ id (PK)         │
                   │key_recycle_bin │     │ key_id (FK)     │
                   ├────────────────┤     │ machine_code    │
                   │ id (PK)        │     │ ip_address      │
                   │ batch_id (IDX) │     │ first_seen      │
                   │ original_key_id│     │ last_heartbeat  │
                   │ key_data (JSON)│     └─────────────────┘
                   │ devices(JSON)  │
                   │ deleted_at     │
                   └────────────────┘
```

### 3.2 表结构说明

#### admin — 管理员表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| username | VARCHAR(64) | 用户名（唯一） |
| password_hash | VARCHAR(256) | bcrypt 哈希密码 |
| otp_secret | VARCHAR(32) | TOTP 密钥（NULL = 未启用） |

#### settings — 系统设置表（Key-Value）
| 字段 | 类型 | 说明 |
|------|------|------|
| key | VARCHAR(64) PK | 配置键 |
| value | TEXT | 配置值 |

预置键：
- `api_prefix` — 管理端 API 路由前缀（默认 `/api`）
- `heartbeat_threshold_minutes` — 在线心跳阈值（默认 `5`）
- `otp_max_attempts` — OTP 最大错误次数（默认 `5`）
- `otp_lockout_minutes` — OTP 锁定时间（默认 `2`）

#### applications — 应用表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| name | VARCHAR(128) | 应用名称 |
| app_code | VARCHAR(32) UQ | 应用编号（自动生成，12位大写，可修改） |
| description | TEXT | 应用描述 |
| created_at | DATETIME | 创建时间 |

#### keys — 卡密表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| key_code | VARCHAR(64) UQ | 卡密字符串（支持自定义前缀/后缀） |
| batch_id | VARCHAR(20) INDEX | 生成批次号 |
| app_id | INTEGER FK | 关联应用 |
| key_type | VARCHAR(32) | day/week/month/quarter/year/permanent/custom |
| device_limit | INTEGER | 最大绑定设备数 |
| status | VARCHAR(16) | active / used / expired / disabled |
| start_time | DATETIME | 首次激活时间（首次使用时设置） |
| expire_time | DATETIME | 过期时间 |
| created_at | DATETIME | 创建时间 |

#### device_bindings — 设备绑定表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| key_id | INTEGER FK | 关联卡密 |
| machine_code | VARCHAR(64) | 设备机器码（32位MD5） |
| ip_address | VARCHAR(45) | 客户端IP |
| first_seen | DATETIME | 首次绑定时间 |
| last_heartbeat | DATETIME | 最近心跳时间 |

#### key_recycle_bin — 回收站表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| batch_id | VARCHAR(32) INDEX | 删除批次号 |
| original_key_id | INTEGER | 原卡密 ID |
| key_data | TEXT | 卡密字段（JSON） |
| devices_data | TEXT | 设备绑定数据（JSON） |
| deleted_at | DATETIME | 删除时间 |

---

## 4. 安全设计

### 4.1 认证体系

| 层级 | 机制 | 说明 |
|------|------|------|
| 管理端登录 | 用户名 + 密码 + TOTP | bcrypt 哈希 + 动态码同框提交，错误统一提示防枚举 |
| 管理端 API | JWT Bearer Token | Access Token 2h + Refresh Token 7d |
| 客户端 API | 卡密 + 机器码 + 应用编号 | 三要素校验，无需登录态 |
| 页面路由 | 前端 JS 检查 token | 无 token → 跳转登录页 |

### 4.2 TOTP 两步验证

- 基于 TOTP 标准（RFC 6238），30 秒刷新
- 启用流程：生成密钥 → 扫码 → 输入动态码确认 → 写入数据库
- 未验证确认前密钥不保存，防止误启用
- 登录时用户名、密码、OTP 同框提交，所有失败返回相同错误信息，无法区分密码错误还是 OTP 错误
- OTP 错误次数和锁定时长可在系统设置中配置（默认 5 次/2 分钟）

### 4.3 动态 API 路由前缀

管理员可在系统设置中修改管理端 API 的前缀。实现原理：

1. 管理端 API 在内部注册为 `/__admin/...`
2. 中间件**阻止**外部直接访问 `/__admin/...`（返回 404）
3. 中间件将 `{配置前缀}/...` **重写**为 `/__admin/...`
4. `/api/client/auth` 和 `/api/prefix` 为固定路径，**不参与**重写

> 效果：即使攻击者通过目录爆破猜到默认路径，如果管理员已将前缀改为自定义值，所有管理接口返回 404，无从定位真实接口。

### 4.4 请求频率限制

| 接口 | 限制 |
|------|------|
| 登录 `/auth/login` | 每 IP 10次/分钟 |
| 客户端认证 `/client/auth` | 每 IP 10次/分钟 |

### 4.5 Docker 安全

- 容器以非 root 用户 `appuser` 运行
- 应用代码属主 root，appuser 只读
- 仅 `/app/data` 目录可写（数据库文件）
- `.dockerignore` 排除敏感文件（.env、.git 等）

---

## 5. 功能设计

### 5.1 仪表盘

- 全局统计：卡密总数、活跃、在线、即将过期、已过期、已禁用、应用数
- 每应用卡片：活跃/在线/绑定设备/将过期/已过期/已禁用

### 5.2 应用管理

- 增删改查，支持搜索
- app_code 可在编辑时修改（客户端需要同步更新）
- 删除前校验是否有关联卡密

### 5.3 卡密管理

- **批量生成**：选应用、类型、数量、设备限制、过期天数、自定义前缀/后缀
- **全字段筛选**：卡密关键词、应用名、批次号、类型、状态、在线状态、过期日期范围、创建日期范围——全部支持模糊搜索和联合查询
- **多选操作**：复选框、全选当页、批量删除
- **批次管理**：每批生成自动分配批次号，支持按批次筛选和一键删除整批
- **导出 CSV**：导出当前筛选结果，一行一设备，含机器码、IP、首次连接、最近心跳
- **设备解绑**：卡密详情页逐设备解绑
- **在线状态**：根据心跳阈值（默认 5 分钟，可配）判定设备是否在线

### 5.4 回收站

- 所有删除操作先进入回收站，按删除批次分组
- 支持整批恢复（含设备绑定数据）、单张恢复、永久删除

### 5.5 系统设置

- API 路由前缀
- 心跳在线阈值（1-1440 分钟）
- OTP 安全限制（最大错误次数 1-20、锁定时间 1-60 分钟）
- 两步验证（启用/禁用/密钥管理）
- 管理员密码修改

---

## 6. 卡密生命周期

```
生成 (status=active, batch_id=xxx)
  │
  │ 客户端首次认证
  ▼
激活 (status=used, start_time=now)
  │
  ├── 正常使用中（每次认证更新心跳）
  │
  ├── 超过有效期 ──→ 过期 (status=expired)
  │
  ├── 管理员禁用 ──→ 禁用 (status=disabled)
  │
  └── 管理员删除 ──→ 进入回收站（可恢复）
```

### 6.1 类型与默认有效期

| 类型 | 默认有效期 | 说明 |
|------|-----------|------|
| day | 1 天 | 天卡 |
| week | 7 天 | 周卡 |
| month | 30 天 | 月卡 |
| quarter | 90 天 | 季卡 |
| year | 365 天 | 年卡 |
| permanent | 永久 | 永不过期 |
| custom | 自定义 | 生成时指定天数 |

> 生成时可额外指定自定义过期天数覆盖类型默认值。

### 6.2 在线状态判定

- `now - last_heartbeat ≤ 阈值` → **在线**
- `now - last_heartbeat > 阈值` → 离线
- 阈值默认 5 分钟，可在设置中调整

---

## 7. 部署说明

### 7.1 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| ADMIN_USERNAME | admin | 管理员用户名（首次启动创建） |
| ADMIN_PASSWORD | admin123 | 管理员密码（**生产务必修改**） |
| JWT_SECRET | change-me... | JWT 签名密钥（**生产务必修改**） |
| DATABASE_URL | sqlite:///./data/app.db | 数据库路径 |
| TZ | Asia/Shanghai | 时区 |

### 7.2 Docker 部署

```bash
# 修改环境变量
vim .env

# 启动
docker-compose up -d

# 访问
http://<服务器IP>:8000
```

数据库文件通过 `./data:/app/data` 持久化，容器删除不丢数据。容器以非 root 用户运行。

### 7.3 导出镜像（离线部署）

```bash
docker build -t key-manager:latest .
docker save key-manager:latest | gzip > key-manager.tar.gz

# 目标机器
docker load < key-manager.tar.gz
docker-compose up -d
```

---

## 8. 项目文件结构

```
卡密管理系统/
├── app/
│   ├── main.py              # 应用入口、中间件、lifespan
│   ├── config.py             # 环境变量配置
│   ├── database.py           # 数据库引擎 & Session
│   ├── models.py             # SQLAlchemy 模型 (6表)
│   ├── schemas.py            # Pydantic 请求/响应模型
│   ├── dependencies.py       # JWT、限流、设置读写
│   ├── routers/
│   │   ├── auth.py           # 管理端登录 + TOTP
│   │   ├── client.py         # 客户端认证 (固定路径)
│   │   ├── applications.py   # 应用管理
│   │   ├── keys.py           # 卡密管理（生成/筛选/批次/导出）
│   │   ├── dashboard.py      # 仪表盘 + 每应用统计
│   │   ├── settings_router.py# 系统设置 + OTP 管理
│   │   ├── recycle.py        # 回收站
│   │   └── pages.py          # Web 页面路由
│   ├── templates/
│   │   ├── base.html         # 基础布局 (侧边栏+导航)
│   │   ├── login.html        # 登录页 (用户名+密码+OTP)
│   │   ├── dashboard.html    # 仪表盘 (全局+每应用)
│   │   ├── applications.html # 应用管理
│   │   ├── keys.html         # 卡密管理 (全字段筛选+批次)
│   │   ├── key_detail.html   # 卡密详情 (设备列表+解绑)
│   │   ├── recycle_bin.html  # 回收站
│   │   └── settings.html     # 系统设置 (OTP管理)
│   └── static/
│       └── tailwind.min.css  # 离线 CSS
├── build_css.py              # CSS 构建脚本
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── .dockerignore
├── .env                      # 环境变量 (不提交Git)
├── .gitignore
├── README.md                 # 项目说明
├── 设计文档.md                # 本文档
└── 客户端调用文档.md          # 客户端认证接口说明
```
